htmllint
========

Getting Started
---------------

To get started, pull in the dependencies:

```
$ npm install
# install gulp cli globally
$ npm install -g gulp
# lint js files
$ gulp lint
# run tests
$ gulp test
$ npm test # this works too
```
